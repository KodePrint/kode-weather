import './Header.css'

const Header = () => {
  return (
    <div className="Header">
      <nav className="Navbar">
        <button>Login</button>
      </nav>
    </div>
  )
}

export default Header